# Meme-Generator
A simple meme generator built with React. Fetches meme templates from the Imgflip API and lets you customize the top and bottom text.  ## Built with - React - Vite - Imgflip API  ## Run it locally ```bash npm install npm run dev ```
